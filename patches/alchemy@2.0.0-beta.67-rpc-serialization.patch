diff --git a/src/Local/RpcSerialization.ts b/src/Local/RpcSerialization.ts
index 5ce2f8c..3f5ef58 100644
--- a/src/Local/RpcSerialization.ts
+++ b/src/Local/RpcSerialization.ts
@@ -1,3 +1,4 @@
 import * as Cause from "effect/Cause";
+import * as Context from "effect/Context";
 import * as Effect from "effect/Effect";
 import * as Exit from "effect/Exit";
@@ -175,2 +176,5 @@ const unwrapRpcStreamHandler = <Args extends Array<any>, Success, Error>(
 const serializeRpcArgs = (value: unknown): unknown => {
+  if (Effect.isEffect(value) || Context.isContext(value)) {
+    return null;
+  }
   if (Redacted.isRedacted(value)) {
